
/*     import React from 'react';
    import { useState } from 'react';

    export default function Prioridad(){
    const [prioridad, setPrioridad] = useState(2);
    const onPriorityChange = (e) => {
        setPrioridad(Number.parseInt(e.target.value));
    }

    return (
        <div>
        <label>Prioridad
        <select id='prioridad' name='Prioridad' value={prioridad} title='Ingresa Prioridad' rows="10" cols="140" onChange={onPriorityChange} >
        <option value={3}>Low</option>
        <option value={2}>Medium</option>
        <option value={1}>High</option>
        </select>
        </label>
        
    </div>
    )
}
 */
